name: warrden

services:
  warrden:
    image: registry.neureka.dev/warrden/warrden:4
    container_name: warrden
    env_file:
      - .env
    networks:
      - warrden
    volumes:
      - data:/app/data
      - ./config.yaml:/config/config.yaml:ro
    restart: unless-stopped

networks:
  warrden:
    name: warrden

volumes:
  data:
