version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - contextcheck
    - errcheck
    - errorlint
    - noctx
    - rowserrcheck
    - sqlclosecheck
    - staticcheck

formatters:
  enable:
    - gofmt
    - goimports
